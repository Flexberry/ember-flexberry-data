# Миграция: utils (дополнительные инструкции)

## remaining utils files

### 1. utils/attributes.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function getAttributes()` function

### 2. utils/backup.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function createBackup()` function

### 3. utils/batch-queries.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function batchQueries()` function

### 4. utils/create.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function createRecord()` function

### 5. utils/enum-functions.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function getEnumValue()` function

### 6. utils/first-load-offline-objects.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function getFirstLoadObjects()` function

### 7. utils/generate-unique-id.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function generateUniqueId()` function

### 8. utils/get-serialized-date-value.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function getSerializedDateValue()` function

### 9. utils/information.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function getInformation()` function

### 10. utils/is-async.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function isAsync()` function

### 11. utils/is-embedded.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function isEmbedded()` function

### 12. utils/is-model-instance.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function isModelInstance()` function

### 13. utils/is-object.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function isObject()` function

### 14. utils/is-uuid.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function isUUID()` function

### 15. utils/model-functions.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function getModelFunction()` function

### 16. utils/queue.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function createQueue()` function

### 17. utils/reload-local-records.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function reloadLocalRecords()` function

### 18. utils/snapshot-transform.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function snapshotTransform()` function

### 19. utils/string-functions.js
- Ember: `EmberObject.extend({...})`
- Next.js: `export function formatString()` function

### 20. app/utils/batch-queries.js (дубликат)
- Ember: `EmberObject.extend({...})`
- Next.js: `export function batchQueries()` function (см. `INSTRUCTIONS_addon-utils-batch-queries.js.md`)

Создать инструкции по аналогии с `INSTRUCTIONS_addon-utils-*.js.md`
