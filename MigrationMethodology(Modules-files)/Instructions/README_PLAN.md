# План завершения миграции

## Текущее состояние
- Всего файлов в scan_result.json: **151**
- Уже обработано: **68**
- Осталось обработать: **83**

## Стратегия завершения

### Недостающие инструкции для модулей:
1. **mixins** (11 файлов) — не хватает инструкций для mixin файлов
2. **query-builder** (12 файлов) — не хватает инструкций для query-builder файлов
3. **stores** (5 файлов) — не хватает инструкций для stores файлов
4. **utils** (20 файлов) — не хватает инструкций для utils файлов
5. **regenerated-serializers** (6 файлов) — не хватает инструкций для regenerated файлов
6. **online-store.js** — специфичный файл для stores модуля

### Дубликаты (уже обработаны как общие инструкции):
- adapters: 4 (2 уникальных + 2 дубликата) → 2 инструкции: odata, offline + дубликаты
- serializers: 28 (14 уникальных + 14 дубликатов) → 3 инструкции (base, odata, special)
- models: 19 (10 уникальных + 9 дубликатов) → 4 инструкции (model, offline, without-validation, special) + дубликаты
- transforms: 12 (6 уникальных + 6 дубликатов) → 4 инструкции (decimal, guid, file, flexberry-enum) + дубликаты
- services: 8 (4 уникальных + 4 дубликата) → 3 инструкции (user, syncer, dexie/offline) + дубликаты
- initializers: 6 (3 уникальных + 3 дубликата) → 3 инструкции + дубликаты
- enums: 4 (2 уникальных + 2 дубликата) → 2 инструкции + дубликаты
- instance-initializers: 2 (1 уникальный + 1 дубликат) → 1 инструкция + дубликат

### Рекомендация
Для быстрого завершения:
- Создать компактные инструкции для каждого файла, которые будут ссылаться на аналогичные уже созданные
- Использовать шаблоны с минимальным описанием

### Завершение
После создания всех инструкций проверить:
- [ ] Всего инструкций = 151 (с учетом дубликатов)
- [ ] Для каждого файла из scan_result.json есть соответствующая инструкция
- [ ] Все инструкции соответствуют формату с чек-листами
